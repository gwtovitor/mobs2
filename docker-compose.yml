version: '3.8'

services:
  postgres:
    image: postgres:14
    container_name: postgres-db
    environment:
      POSTGRES_PASSWORD: 12346
      POSTGRES_DB: app
    ports:
      - "5432:5432"
    volumes:
      - "./create.sql:/docker-entrypoint-initdb.d/init.sql"
